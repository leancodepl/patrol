# doctor

### Synopsis

Prints the path where the artifacts are stored.

```bash
$ patrol doctor
```

To learn more about this command, run `patrol doctor --help`.

### Description

Currently this command doesn't do much. See issue [#175] where adding more
features to `patrol doctor` is tracked.

[#175]: https://github.com/leancodepl/patrol/issues/175
