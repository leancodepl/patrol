# develop

### Synopsis

Runs Flutter integration tests as native tests on devices.

```
patrol test
```

To learn more about this command, run `patrol test --help`.

### Description

The Dart VM starts the entrypoint again.

Only the "Dart side" of your app is restarted.

TODO:

- permissions
- file system
- native state
