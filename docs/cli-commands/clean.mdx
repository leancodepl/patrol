# clean

This command removes the directory pointed to by `$PATROL_CACHE` environment
variable, where artifacts and logs are stored.

If `$PATROL_CACHE` environment variable doesn't exist, it defaults to
`$HOME/.patrol`.
