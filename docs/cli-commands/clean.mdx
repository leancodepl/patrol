# clean

This command removes the directory pointed to by `$MAESTRO_CACHE` environment
variable, where artifacts and logs are stored.

If `$MAESTRO_CACHE` environment variable doesn't exist, it defaults to
`$HOME/.maestro`.
