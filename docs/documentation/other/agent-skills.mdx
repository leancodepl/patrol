---
title: Agent skills
description: AI agent skills that teach coding agents how to write Patrol tests
---

Patrol ships [agent skills](https://github.com/leancodepl/patrol/tree/master/skills) — focused,
on-demand instructions in the open [Agent Skills](https://agentskills.io/) format (`SKILL.md`) that
teach an AI coding agent *how* to write Patrol tests following best practices. They complement
[Patrol MCP](/documentation/other/patrol-mcp): the MCP gives the agent the tools to run tests, while
a skill teaches it how to use those tools to write tests well.

The skills ship inside the `patrol` package, so you can install them from your dependency tree
with the Dart [`skills`](https://pub.dev/packages/skills) CLI — here for Claude Code:

```
dart run skills@ get --agent claude -p patrol --all
```

Use `--agent cursor` for Cursor, or `--agent generic` for `.agents/skills/` (Antigravity, Gemini
CLI and others). Rerun it after upgrading `patrol` to keep the skills in sync with your version.

See the [skills catalog in the Patrol repo](https://github.com/leancodepl/patrol/tree/master/skills)
for the available skills and more install options.
