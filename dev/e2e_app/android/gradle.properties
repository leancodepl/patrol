org.gradle.jvmargs=-Xmx3512M
android.useAndroidX=true
# Kotlin 2.3.x incremental compilation fails on Windows CI with
# "Could not close incremental caches ... *.tab" file-locking errors.
# Incremental compilation gives no benefit on clean CI builds, so disable it.
kotlin.incremental=false
