package pl.leancode.patrol.generated

/**
 * Stands in for the class `patrol build android --emit-test-manifest` writes into the app's
 * androidTest source set. Only its fully-qualified name matters here.
 */
class PatrolGeneratedTests
