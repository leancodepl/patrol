import 'package:flutter_test/flutter_test.dart';
import 'package:patrol/patrol.dart';

void main() {
  group('patrolTest()', () {
    patrolTest(
      'does not initialize bindings if nativeAutomation is not enabled',
      () async {
        
      },
    );
  });
}
