import 'package:flutter/widgets.dart';

class PatrolDevToolsExtension extends StatelessWidget {
  const PatrolDevToolsExtension({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
